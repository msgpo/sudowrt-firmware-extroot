The sfirmware builder for the sudowrt extroot firmware.

This firmware is a very minimal. Its only job is to run the same kernel version as normal sudowrt and switch to an ext4 root filesystem on an external usb storage device.
